import React from 'react'
import ReactDOM from 'react-dom/client'
import Layoud from '../src/componets/Layoud'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
  
  </React.StrictMode>,
)
